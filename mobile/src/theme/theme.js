import { colors } from "@theme/colors";

export const theme = {
    colors: {
        ...colors,
        primary: colors.primaryGreen,
        secondary: colors.darkGray,
        background: colors.backgroundDark,
        text: colors.text,
        success: colors.secondary,
        error: colors.danger,
        tabBarActive: colors.primaryGreen,
        tabBarInactive: colors.white,
    },
};
